---
description: 後端資料庫使用 Prisma ORM 的約定
alwaysApply: true
---

# 後端：Prisma 與資料庫

## ORM

- 使用 **Prisma 7** 作為 ORM（非「插件」；取代直接使用 `pg` 撰寫 SQL）。
- Schema 定義在 `prisma/schema.prisma`；連線 URL 在 `prisma.config.ts`（`DATABASE_URL`）。
- 程式中一律透過 `src/lib/db.ts` 匯出的 `prisma` 單例存取資料，勿再建立額外 PrismaClient 或直接使用 `pg` 做業務查詢。

## 新增／修改資料表

1. 編輯 `prisma/schema.prisma`（model、enum、關聯）。
2. 產生 migration：
   - **優先**用 `npm run db:migrate:dev -- --name 描述` 讓 Prisma 自動產生 SQL（表名／FK 會與 schema 一致）。
   - 若手動撰寫 `prisma/migrations/.../migration.sql`，須與既有 migration 對齊：本專案部分表名為 **PascalCase**（如 `"User"`、`"Project"`），FK 須寫 `REFERENCES "User"("id")`，**勿**寫 `users`、`projects`；可對照 `prisma/migrations` 內已有檔案。
3. **必須在同一輪任務內套用至資料庫**，不得只產檔就結束：
   - 開發／本機：若剛用 `migrate:dev` 產生，已一併套用；若只新增了 migration 檔，執行 `npx prisma migrate deploy`（需 DATABASE_URL 可連）。
   - 執行後**確認**無 P3009／P2022 等錯誤；若有失敗，當輪排解（resolve、修正 SQL、重跑）直至 `migrate status` 為 up to date。
4. 執行 `npx prisma generate`（若 migrate:dev 已跑過可略），確認 Client 與 schema 一致。

**規範：** 每次變更 schema 後，由 agent 直接完成「產生 migration → 套用至 DB → generate」，確保資料庫與 Prisma Client 同步，**不讓使用者事後才發現欄位不存在或來回排錯**。

## 查詢與寫入

- 使用 `prisma.*.findMany`、`findUnique`、`create`、`update`、`delete` 等，並以 `select` 控制回傳欄位（勿回傳密碼等敏感欄位）。
- 驗證輸入使用 Zod（`src/schemas/`），再傳入 Prisma。

## 人員與專案

- **User**（人員）、**Project**（專案）、**ProjectMember**（專案成員）、**Tenant**（租戶）已定義於 schema；API 見 `docs/backend-prisma-api.md`。
- 之後加功能（例如專案成員 CRUD、權限過濾）時，仍以 Prisma 查詢，並在 route 或 service 層做權限檢查。
