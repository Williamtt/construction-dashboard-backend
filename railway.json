{
  "$schema": "https://schema.railway.app/railway.schema.json",
  "deploy": {
    "preDeployCommand": "npx prisma migrate resolve --rolled-back 20260403140000_add_contract_no_and_owner_agency_to_project || true && npm run db:migrate"
  }
}
